{
  "name": "diskoko",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check workspace.js && node --check account.js && node --check discord-bot.js && node --check lib/workspace-api.js",
    "test": "node --test tests/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "connect-pg-simple": "^10.0.0",
    "discord.js": "^14.22.1",
    "express": "^5.1.0",
    "express-session": "^1.18.1",
    "nodemailer": "^10.0.10",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  }
}
